#!/usr/bin/env sh
# Conventional Commits, checked before the commit exists rather than in CI.
# These repositories squash-merge, so the PR or MR title becomes the commit
# subject too; a prose title breaks the next push to the target branch.
exec yarn --silent lint:commit --edit "$1"
