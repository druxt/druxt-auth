# Named exceptions, each with a reason. Nothing goes here to make a scan pass.
#
# Empty on purpose. The one standing exception, the example's OAuth client_id,
# is a path allowlist in .gitleaks.toml scoped to the single rule that fires.
# A fingerprint names one commit, and the same value sits in several, so which
# of them a scan reached depended on the clone depth.
